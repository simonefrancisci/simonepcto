#include <stdio.h>

int main()
{
    int a;
    int b;
    
    printf("ïnserisci i due numeri \n")
    scanf ("%d %d", &a, &b);
    int divisione = a \ b;
    printf("ïl risultato della divisione è: %d\n", divisione);
    
}